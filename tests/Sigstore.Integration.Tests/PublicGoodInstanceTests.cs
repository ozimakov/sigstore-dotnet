namespace Sigstore.Integration.Tests;

public sealed class PublicGoodInstanceTests
{
    [Fact(Skip = "Requires network access to the Sigstore Public Good Instance and a real bundle fixture.")]
    public void Given_LiveBundle_When_Verifying_Then_Succeeds()
    {
        Assert.True(true);
    }
}
